# {VENUE} 提交用摘要（纯文本）

> 用途：填入投稿系统 "Abstract*" 框。
> 规则：见 `references/{venue}.md`（如 RAS/PaperCept：纯文本，仅识别 `<b> <i> <sub> <sup>`，超长截断）。
> 开源链接状态：{已提供：<url> / 用户明确不提供 / 待定（先不要定稿）}

---

## 可直接复制（plain text）

{单段纯文本摘要，已去除全部 LaTeX；数学用 <sub>/<sup>，强调用 <b>/<i>；URL 写成裸地址}

---

## 备注

- **与论文一致**：内容同 `Sections/0_Abstract.tex` 的编译版，仅做纯文本化处理（去 `\url{}`、去 `\cite`/标签/注释）。
- **用到的标签**：{无 / 列出实际用到的，如 F<sub>ext</sub>、x<sup>2</sup>}。
- **长度**：{N} 字符 / {M} 词（单段无换行）；若系统提示截断，{给出可删减的句子，如末尾源码链接}。
